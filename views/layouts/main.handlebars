<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
    <link rel="stylesheet" href="/public/css/style.css">
    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

<div class="wrapper">
    <header>
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            <a href="/dashboard" class="navbar-item is-hoverable pt-1">Dashboard</a>
            {{else}}
            <a href="/login" class="navbar-item is-hoverable pt-1">Login</a>
            {{/if}}
        </nav>
        {{#if email}}
        <h1 class="has-text-black px-3 py-3 has-text-weight-bold">Date Generator: {{email}}</h1>
        {{else}}
        <h1 class="has-text-black px-3 py-3 has-text-weight-bold">Date Generator</h1>
        {{/if}}
    </header>

    <main>
        {{{ body }}}
    </main>

    <footer class="footer rows pb-2">
        <div class="row-is-full content has-text-centered">
            <p>
                <a href="/">Homepage</a>{{#if loggedIn}} |
                <a href="/dashboard/">Dashboard</a> |
                <a href="/dashboard/create/">Create New Post</a>
                {{else}}
                | <a href="/login/">Login</a>
                {{/if}}
            </p>
        </div>
        <nav>
            {{#if loggedIn}}
            <button id="logout" class="button" style="background-color: rgb(247, 0, 255); color: white;">Logout</button>
            {{else}}
            <a href="/login" class="button" style="background-color: rgb(65, 225, 92); color: white;">Login</a>
            {{/if}}
        </nav>
        <div class="row-is-full content has-text-centered">
        </div>
    </footer>
</div>

{{#if loggedIn}}
<script src="/project-2/public/js/logout.js"></script>
{{/if}}
</body>

</html>